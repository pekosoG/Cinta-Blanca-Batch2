// Iniciamos una funcion llamada suma que recibe dos variables, {a,b}.
function suma(a,b) {
  return a+b;
  // Retornamos la suma de las dos variables recibidas.
};

// Exportamos lo que retorna la funcion suma.
module.exports = suma;
